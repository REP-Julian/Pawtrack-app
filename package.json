{
  "name": "pawtrack",
  "private": true,
  "workspaces": [
    "pawtrack_frontend"
  ],
  "scripts": {
    "dev": "npm --prefix pawtrack_frontend run dev",
    "build": "cd pawtrack_frontend && npm install && npm run build && node -e \"const fs = require('fs'); fs.cpSync('dist', '../dist', { recursive: true }); fs.cpSync('dist', '..', { recursive: true });\"",
    "preview": "npm --prefix pawtrack_frontend run preview",
    "start": "npm --prefix pawtrack_frontend run dev"
  }
}
