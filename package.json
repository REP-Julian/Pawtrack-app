{
  "name": "pawtrack",
  "private": true,
  "workspaces": [
    "pawtrack_frontend"
  ],
  "scripts": {
    "build": "cd pawtrack_frontend && npm install --include=optional && npm run build && node -e \"const fs = require('fs'); fs.cpSync('dist', '../dist', { recursive: true });\"",
    "start": "npm --prefix pawtrack_frontend run preview"
  },
  "optionalDependencies": {
    "@rolldown/binding-linux-x64-musl": "1.2.9",
    "@rolldown/binding-linux-x64-gnu": "1.2.9"
  }
}
